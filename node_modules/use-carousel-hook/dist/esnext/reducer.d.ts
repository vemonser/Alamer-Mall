import type { Interaction } from './useCarousel';
declare type ActionOption = {
    interaction: Interaction;
};
declare type Action = (ActionOption & {
    type: 'increment';
    maxIndex: number;
    amount: number;
}) | (ActionOption & {
    type: 'decrement';
    maxIndex: number;
    amount: number;
}) | (ActionOption & {
    type: 'reset';
}) | (ActionOption & {
    type: 'set';
    current: number;
});
interface State {
    current: number;
    interaction: Interaction;
}
declare const reducer: (state: State, action: Action) => State;
export default reducer;
